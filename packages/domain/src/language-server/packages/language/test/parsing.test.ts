import { describe } from "vitest";

/*
let services: ReturnType<typeof createAxiomLambdaLspServices>;
let parse:    ReturnType<typeof parseHelper<Axiom>>;
let document: LangiumDocument<Axiom> | undefined;

beforeAll(async () => {
    services = createAxiomLambdaLspServices(EmptyFileSystem);
    parse = parseHelper<Axiom>(services.AxiomLambdaLsp);

    // activate the following if your linking test requires elements from a built-in library, for example
    // await services.shared.workspace.WorkspaceManager.initializeWorkspace([]);
});
*/

describe('Parsing tests', () => {

    // TODO: Add parsing tests
});
