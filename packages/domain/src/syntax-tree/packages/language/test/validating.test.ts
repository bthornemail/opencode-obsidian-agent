import { describe } from "vitest";

/*
let services: ReturnType<typeof createAxiomLambdaSyntaxTreeServices>;
let parse:    ReturnType<typeof parseHelper<Entity>>;
let document: LangiumDocument<Entity> | undefined;

beforeAll(async () => {
    services = createAxiomLambdaSyntaxTreeServices(EmptyFileSystem);
    const doParse = parseHelper<Entity>(services.AxiomLambdaSyntaxTree);
    parse = (input: string) => doParse(input, { validation: true });

    // activate the following if your linking test requires elements from a built-in library, for example
    // await services.shared.workspace.WorkspaceManager.initializeWorkspace([]);
});
*/

describe('Validating', () => {

    // TODO: Add validation tests
});
